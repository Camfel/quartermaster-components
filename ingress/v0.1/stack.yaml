# Quartermaster Ingress component — v0.1 (DEPRECATED)
# Use the Caddy component instead.  This nginx-based ingress is
# superseded by the caddy component which auto-generates config
# and handles Let's Encrypt TLS automatically.
version: "1"
kind: Stack
metadata:
  name: qm-ingress
spec:
  services: []
